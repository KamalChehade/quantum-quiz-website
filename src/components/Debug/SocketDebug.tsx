import React from 'react';

// SocketDebug temporarily disabled — uncomment the original implementation if you need
// local debug info (socket id, base/path, connection status). Keeping a no-op
// component so imports remain valid across the app.
export const SocketDebug: React.FC = () => null;
export default SocketDebug;
